language: node_js
node_js:
  - 13.3.0
script: echo "Testing temporarily disabled"
before_install:
  - npm i -g noodle-deploy
deploy:
  provider: script
  script: npm run deploy -- -s $AES_PASS
  on:
    branch: master
branches:
  only:
    - master
    - development
